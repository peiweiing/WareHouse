```
<pre>保持代码里的格式显示</pre>
<embed>引入flash</embed>
<marquee>字幕滚动</marquee>
overflow:hidden/*溢出隐藏*/
ul,li{list-style:none;}/*去掉小黑点*/
a{text-decoration}/*去掉下划线*/
line-height:xxpx;/*行高等于盒子高度，字体就垂直居中*/
text-align:center;/*行高字体左右居中*/
vertial-align:middle;/*垂直居中*/
opacity:0.5/*不透明度*/
rgb a{0~255,0~255,0~255,0~1}/*最后一个属性值为不透明度*/

a:link{color:...}
a:visited{color:...}
a:hover{color:...}
a:active{color:...}
:before{content:"..."}在该类前面
:after{content:"..."}在该类后面
.clear{clear:both;}/*清楚浮动带来的影响*/
visitility:hidden/*使对象不可见，但仍然占用位置*/
display:none;/*消失隐藏，不占用位置*/
buckground-size:{左右，上下}
min-width:...px;/*固定页面最小宽度*/
min-height:...px;/*固定页面最小高度*/
max-width:...px;/*固定页面最大宽度*/
max-height:...px;/*固定页面最大高度*/
transition:1s/*秒*/
box-sizing:border-box;/*转换为怪异盒子*/
box-sizing:conten-box;/*转换为标准盒子*/
```

